{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}Admin rozhranie{% endblock %}

{% block main %}
    <div class="content-panel">
        <div class="content-panel-body">
            <div class="mb-4">
                <p class="text-muted mb-1">Prihlásený používateľ</p>
                <div class="fs-5 fw-semibold">{{ app.user.fullName }}</div>
            </div>

            <div class="alert alert-success mb-0" role="alert">
                Prihlásenie cez Symfony Security je aktívne a administrácia beží na rozložení EasyAdminu.
            </div>
        </div>
    </div>
{% endblock %}
